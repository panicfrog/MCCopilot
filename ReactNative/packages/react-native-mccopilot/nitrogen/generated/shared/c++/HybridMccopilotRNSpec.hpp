///
/// HybridMccopilotRNSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `Argon2Params` to properly resolve imports.
namespace margelo::nitro::mccopilot { struct Argon2Params; }

#include <string>
#include <NitroModules/ArrayBuffer.hpp>
#include <optional>
#include "Argon2Params.hpp"

namespace margelo::nitro::mccopilot {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `MccopilotRN`
   * Inherit this class to create instances of `HybridMccopilotRNSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridMccopilotRN: public HybridMccopilotRNSpec {
   * public:
   *   HybridMccopilotRN(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridMccopilotRNSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridMccopilotRNSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridMccopilotRNSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::string getVersion() = 0;
      virtual std::string cryptoHash(const std::string& algorithm, const std::shared_ptr<ArrayBuffer>& data) = 0;
      virtual std::shared_ptr<ArrayBuffer> cryptoHkdfDerive(const std::shared_ptr<ArrayBuffer>& salt, const std::shared_ptr<ArrayBuffer>& ikm, const std::optional<std::shared_ptr<ArrayBuffer>>& info, double outputLength) = 0;
      virtual std::shared_ptr<ArrayBuffer> cryptoPbkdf2Derive(const std::shared_ptr<ArrayBuffer>& password, const std::shared_ptr<ArrayBuffer>& salt, double iterations, double outputLength) = 0;
      virtual std::shared_ptr<ArrayBuffer> cryptoAesCbcEncrypt(const std::shared_ptr<ArrayBuffer>& key, const std::shared_ptr<ArrayBuffer>& iv, const std::shared_ptr<ArrayBuffer>& plaintext) = 0;
      virtual std::shared_ptr<ArrayBuffer> cryptoAesCbcDecrypt(const std::shared_ptr<ArrayBuffer>& key, const std::shared_ptr<ArrayBuffer>& iv, const std::shared_ptr<ArrayBuffer>& ciphertext) = 0;
      virtual std::string cryptoAesCtr(const std::shared_ptr<ArrayBuffer>& key, const std::shared_ptr<ArrayBuffer>& nonce, const std::shared_ptr<ArrayBuffer>& data) = 0;
      virtual std::shared_ptr<ArrayBuffer> cryptoAesGcmEncrypt(const std::shared_ptr<ArrayBuffer>& key, const std::shared_ptr<ArrayBuffer>& nonce, const std::shared_ptr<ArrayBuffer>& plaintext, const std::optional<std::shared_ptr<ArrayBuffer>>& aad) = 0;
      virtual std::shared_ptr<ArrayBuffer> cryptoAesGcmDecrypt(const std::shared_ptr<ArrayBuffer>& key, const std::shared_ptr<ArrayBuffer>& nonce, const std::shared_ptr<ArrayBuffer>& ciphertext, const std::optional<std::shared_ptr<ArrayBuffer>>& aad) = 0;
      virtual std::shared_ptr<ArrayBuffer> cryptoAesGcmGenerateNonce() = 0;
      virtual std::shared_ptr<ArrayBuffer> cryptoAesGenerateIv() = 0;
      virtual std::string cryptoArgon2Hash(const std::string& password, const std::optional<Argon2Params>& params) = 0;
      virtual std::shared_ptr<ArrayBuffer> cryptoArgon2HashWithSalt(const std::shared_ptr<ArrayBuffer>& password, const std::shared_ptr<ArrayBuffer>& salt, const std::optional<Argon2Params>& params) = 0;
      virtual bool cryptoArgon2Verify(const std::string& password, const std::string& hash) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "MccopilotRN";
  };

} // namespace margelo::nitro::mccopilot
